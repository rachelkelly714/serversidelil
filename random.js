function theMod(score) {
    let modifier; 
     if (score >=10) {
  modifier = Math.floor((10-10) / 2); 
     } else {
  modifier = Math.floor((20 - 11) /2); 
  }
    return modifier; 
  console.log(theMod)
  }
  